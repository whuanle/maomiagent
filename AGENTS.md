# MaomiAgent Workspace Rules

- 资源管理类页面默认直接进入筛选、主操作、主列表，禁止首屏工作台、概览卡、统计卡、眉标和说明性废话文案。
- 面向终端用户的页面文案必须只服务当前任务，禁止出现“产品会帮你做什么”的自我介绍式、能力总述式、导览式说明；空状态默认直接给操作入口，不写主观抒情和自夸文案。
- 面向终端用户的启动态、加载态、等待态文案必须是最小必要提示，只能写“正在加载”“请稍候”“即将完成”这类当前状态；禁止出现 sidecar、runtime、握手、通道、bootstrap、模块、架构、设计、实现原理等内部机制说明，更禁止借等待文案向用户解释系统如何工作。
- 面向终端用户如果需要提示、告警、错误反馈，必须使用 `message` 或通知类能力；严禁在页面内放置占位组件（如 `Alert`、提示条、警告横幅）去挤占布局做提示。
- 资源管理类页面默认采用“工具栏 + 主表格”的单列表骨架：首屏直接进入筛选、主操作、主列表，禁止 dashboard/workbench、概览卡、统计卡、说明卡、状态 Tabs、右侧详情栏和表格外常驻状态面板，除非模块文档明确声明例外。
- 资源管理类页面工具栏必须从左到右顺排，不要用 `space-between` 硬拆筛选区和操作区；搜索、筛选、次操作、主操作按阅读顺序排列，控件高度保持一致。
- 资源管理类页面主表格必须吃满剩余高度，状态列在操作列前，操作列使用轻量图标按钮；关键属性直接进列，不要依赖侧栏、inspector 或表格外状态区补信息。
- 所有模态窗体必须避让窗口标题栏，顶部不得压住标题栏；默认预留标题栏高度加安全间距，禁止弹窗直接顶到窗口最上沿。
- 详情、检查这类长内容模态窗体默认使用 `80vh` 作为高度上限；超出后只能让内部内容区或列表区滚动，不允许整窗继续撑高，更不允许因此遮住标题栏。
- 技能页必须参考日志页的版式骨架：Ant Design 组件为主，页面结构限定为 Tabs 加工具栏加主表格，不得回退到 dashboard/workbench 设计，也不要在页面搞个详情区。
- 禁止在页面实现里引入 TailwindCSS 风格的工具类心智；样式应使用现有语义化类名并贴合当前主题变量。
- 新增或改造页面前，不要参考现有同类页面实现和 repo memory，不要考虑优先复用已有结构与交互模式！要完全重构！
- 一个模块的页面代码，需要放到一个 components 目录单独放置，避免混乱。
