# maomiagent

Install the desktop launcher package globally:

```bash
npm i -g maomiagent
```

Launch MaomiAgent:

```bash
maomi-agent
```

Update the installed package:

```bash
npm update -g maomiagent
```
