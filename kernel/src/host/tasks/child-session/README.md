# MaomiKernel Support Child Session

`support/child-session` 承载通用 child session 执行能力。

当前阶段包含：

- `ChildSessionRunner`
- `DefaultChildSessionResultNormalizer`

这里不负责：

- task / todo / checkpoint 的业务语义
- agent / workspace / tool catalog 装配
